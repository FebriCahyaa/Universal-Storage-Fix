# Meta-Module adapter

No Meta-Module product or stable interface was selected. The adapter detects that unsupported state and returns no capability. It performs no lifecycle mutation and cannot bootloop the device. A future adapter must cite public interface documentation, add capability detection and device tests, then be enabled explicitly.
