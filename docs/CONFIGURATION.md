# Configuration

The private configuration file is created with mode 0600 under module state. Supported values are `enabled`, `log_level`, `zygisk_mode`, `package_scope`, and `update_channel`. Values are advisory until validated by the runtime; no configuration accepts commands or paths.
