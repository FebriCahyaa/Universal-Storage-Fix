# Testing

`make test` runs C++ policy assertions, shell confirmation/path tests, and update-schema validation. `make checksums` packages and hashes the module. Rooted-device integration tests, MediaStore/SAF app-context tests, and Android arm64 ABI tests are required before calling the corresponding capabilities runtime-validated.
